<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
  <% if current_user.is_admin? && !current_user.current_user?(user) %>
    | <%= link_to t("delete"), admins_user_path(user), method: :delete,
    data: {confirm: t("confirm")} %>
  <% end %>
</li>
